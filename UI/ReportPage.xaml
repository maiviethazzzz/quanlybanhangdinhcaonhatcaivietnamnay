<Page x:Class="QuanLyBanHang.UI.ReportPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Báo cáo">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Bộ lọc ngày -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Từ ngày:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="dpFrom" Width="150" Margin="0,0,20,0"/>
            <TextBlock Text="Đến ngày:" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="dpTo" Width="150"/>
        </StackPanel>
        
        <!-- Nút báo cáo -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Danh sách hóa đơn" Padding="15,8" Margin="0,0,10,0" Click="btnInvoices_Click"/>
            <Button Content="Doanh số theo nhân viên" Padding="15,8" Margin="0,0,10,0" Click="btnSalesByStaff_Click"/>
            <Button Content="Doanh số theo ngày" Padding="15,8" Margin="0,0,10,0" Click="btnDailySales_Click"/>
            <Button Content="Cảnh báo tồn kho" Padding="15,8" Click="btnLowStock_Click"/>
        </StackPanel>
        
        <!-- Kết quả -->
        <GroupBox Grid.Row="2" Header="Kết quả">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <DataGrid x:Name="dgReport" Grid.Row="0" AutoGenerateColumns="True" IsReadOnly="True"/>
                
                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,10,0,0">
                    <TextBlock x:Name="lblSummary" FontWeight="Bold" FontSize="14"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</Page>
